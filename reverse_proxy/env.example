# JWT Secret for authentication (REQUIRED - change this!)
JWT_SECRET=your_super_secret_key_here_change_me

# Logging level
LOG_LEVEL=INFO

# SSL Certificates (Optional)
# Place cert.pem and key.pem in ./certs/ directory to enable HTTPS
# The server will automatically detect and use them 